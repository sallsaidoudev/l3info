Builtin commands available from the console
 (console is opened using the tilde ~ key):

General:
  help [exp]
  set id exp
  exec file
  echo text
  toggle variable [value]          # eg. toggle input_super_mode console
  dump_log [file]
  alias new_name command

Input commands:
  quit
  bind_global key command
  bind_mode key mode command
  bind_action key action [local_pl_no]
  action action start [local_pl_no]

Menu commands:
  menu_command
  menu_var
  menu_jump
  menu_rebuild

Net commands:
  server [port]
  connect [hostname [port]]
  disconnect [local_pl_no]

Video commands:
  screenshot